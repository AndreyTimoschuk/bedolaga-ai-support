[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
markers =
    contract: redis/postgres/group-bot contract tests (no n8n required)
    e2e: full stack tests (n8n + webhook + tunnel)
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
